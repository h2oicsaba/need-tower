# Need Tower – PRD environment (.env.production.local example)
#
# Használat (lokális prod preview):
# 1) Másold ezt a fájlt .env.production.local néven ugyanebbe a könyvtárba (apps/web/).
# 2) Töltsd ki a PRD Supabase projekt adataival.
# 3) Lokális prod mód: pnpm --filter web build && pnpm --filter web start
#
# MEGJEGYZÉS: Vercel NEM olvassa a repo-beli .env fájlokat éles futáskor.
# A PRD értékeket a Vercel Project Settings → Environment Variables alatt add meg.

# Kötelező kliens env változók (PRD Supabase projekt):
NEXT_PUBLIC_SUPABASE_URL=https://your-prd-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-prd-anon-key

# Opcionális: port felülírása lokális start-hoz
# PORT=3000

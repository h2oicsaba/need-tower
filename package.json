{
  "name": "need-tower",
  "private": true,
  "packageManager": "pnpm@9.7.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "validate:answers": "node scripts/validate_answers_lines.js"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}
